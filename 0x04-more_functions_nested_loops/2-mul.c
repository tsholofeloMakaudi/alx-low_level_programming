#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the 1st ingeter
 * @b: the second integer for the equation
 * Return: the multiple of two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
